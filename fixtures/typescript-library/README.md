# TypeScript CLI/library Reference Fixture

Small non-web source fixture for the K0 contract harness. `anyam.json`
declares the portable check/build Actions, Evidence Verifiers, typed package
Artifact, and generic release-assets Target used by the local Release journey.
