/**
 * Repository-level compatibility entry point for the customer-Realm HTTP
 * client. The implementation lives with the published CLI so its package
 * build remains self-contained.
 */
export * from "../../packages/create-anyam/src/realm-authority-client.ts";
