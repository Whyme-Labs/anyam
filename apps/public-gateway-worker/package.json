{
  "name": "@anyam/public-gateway-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run typecheck && node ../../node_modules/wrangler/bin/wrangler.js deploy --dry-run --config wrangler.example.jsonc --outdir=dist"
  }
}
